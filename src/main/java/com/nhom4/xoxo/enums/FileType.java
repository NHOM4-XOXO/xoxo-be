package com.nhom4.xoxo.enums;

public enum FileType {
    IMAGE,      // Hình ảnh (jpg, png, gif, webp)
    VIDEO,      // Video (mp4, avi, mov, webm)
    AUDIO,      // Âm thanh (mp3, wav, aac, ogg)
    DOCUMENT,   // Tài liệu (pdf, doc, docx, txt)
    SPREADSHEET, // Bảng tính (xls, xlsx, csv)
    PRESENTATION, // Trình chiếu (ppt, pptx)
    ARCHIVE,    // File nén (zip, rar, 7z)
    CODE,       // Mã nguồn (java, py, js, html)
    OTHER       // Loại khác
}
